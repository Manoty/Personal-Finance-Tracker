name: 'personal_finance_tracker'
version: '1.0.0'
config-version: 2

profile: 'personal_finance_tracker'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  personal_finance_tracker:
    staging:
      +materialized: view
    marts:
      +materialized: table

seeds:
  personal_finance_tracker:
    +quote_columns: false